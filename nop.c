#include "monty.h"

/**
* nop - Does nothing.
* @head: Stack head.
* @stack: A pointer to the top of the stack.
*/

void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
